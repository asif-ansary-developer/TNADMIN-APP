import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ViewStatusAdmin2Page } from './view-status-admin-2.page';

describe('ViewStatusAdmin2Page', () => {
  let component: ViewStatusAdmin2Page;
  let fixture: ComponentFixture<ViewStatusAdmin2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ViewStatusAdmin2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
